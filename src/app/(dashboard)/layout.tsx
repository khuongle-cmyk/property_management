import type { ReactNode } from "react";

/** Route group for workspace pages (URL unchanged). Chrome + AppNav come from root ConditionalWorkspaceChrome. */
export default function DashboardRouteGroupLayout({ children }: { children: ReactNode }) {
  return children;
}
